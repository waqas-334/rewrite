import React from "react";
import Icon from "./assets/translations.svg";
import { SvgProps } from "react-native-svg";

const TranslationsIcon = (props: SvgProps) => <Icon {...props} />;

export default TranslationsIcon;
