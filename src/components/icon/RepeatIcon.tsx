import React from "react";
import Icon from "./assets/repeat.svg";
import { SvgProps } from "react-native-svg";

const RepeatIcon = (props: SvgProps) => <Icon {...props} />;

export default RepeatIcon;
