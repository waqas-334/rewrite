import React from "react";
import Icon from "./assets/signature.svg";
import { SvgProps } from "react-native-svg";

const SignatureIcon = (props: SvgProps) => <Icon {...props} />;

export default SignatureIcon;
